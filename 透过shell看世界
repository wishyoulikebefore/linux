扩展

路径名扩展：使用通配符
#示例
echo D*
echo *s
echo [[:upper:]]
ls -d .[!.]?*

波浪线扩展：用在一个单词的开头，将被扩展为指定用户的主目录名；若未指定，则扩展为当前用户的主目录
#示例
echo ~
echo ~foo

算术扩展
#示例
echo $((2+2))
echo $(((5**2)*3))

花括号扩展：创建多种文本字符串，用于创建一系列的文件或者目录
#示例
echo Front-{A,B,C}-Back

echo Number_{1..5}

echo a{A{1,2},B{3,4}}b

参数扩展
#示例
echo $USER

命令替换：把一个命令的输出作为一个扩展模式使用
#示例
echo $(ls)

引用
双引号：将文本放入双引号，除“$”,"\","'"外，所有特殊字符都会失去意义，即单词分割、路径名扩展、波浪线扩展和花括号扩展都讲失效
        而参数扩展、算术扩展和命令替换依然有效
#示例
echo $(cal)

echo "$(cal)"


单引号：抑制所有扩展

转义字符

